node_modules
dist
.DS_Store
*.local

# Saved source pages (Safari webarchives) kept locally for reference only
*.webarchive

# Downloaded TDS/MSDS/RoHS PDFs kept locally for reference only
temp_docs/
